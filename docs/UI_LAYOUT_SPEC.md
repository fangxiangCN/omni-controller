# Omni Controller UI 布局详细规范

## 1. 布局架构总览

### 1.1 屏幕分区

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                    Title Bar                                 │
│  [App Icon] Omni Controller                          [Min] [Max] [Close]    │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌──────────────┬──────────────────────────────────────┬─────────────────┐  │
│  │              │                                      │                 │  │
│  │   Device     │                                      │   Inspector     │  │
│  │   Panel      │       Main Workspace                 │   Panel         │  │
│  │   240px      │       (动态宽度)                      │   280px         │  │
│  │   (可折叠)   │                                      │   (可折叠)      │  │
│  │              │                                      │                 │  │
│  │              │  ┌──────────────────────────────┐   │                 │  │
│  │              │  │                              │   │                 │  │
│  │              │  │     Content Area             │   │   Properties    │  │
│  │              │  │                              │   │   / Timeline    │  │
│  │              │  │     - Playground             │   │   / Details     │  │
│  │              │  │     - Report                 │   │                 │  │
│  │              │  │     - Recorder               │   │                 │  │
│  │              │  │                              │   │                 │  │
│  │              │  └──────────────────────────────┘   │                 │  │
│  │              │                                      │                 │  │
│  │              │  ┌──────────────────────────────┐   │                 │  │
│  │              │  │      Input / Timeline        │   │                 │  │
│  │              │  └──────────────────────────────┘   │                 │  │
│  │              │                                      │                 │  │
│  └──────────────┴──────────────────────────────────────┴─────────────────┘  │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │  Status Bar - 32px                                                      ││
│  │  [🟢] [iPhone 15] [393×852] [GPT-4o] [12 tasks] [⚙️]                   ││
│  └─────────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 尺寸规范

| 区域 | 默认宽度/高度 | 最小尺寸 | 最大尺寸 | 可折叠 |
|------|--------------|----------|----------|--------|
| Device Panel | 240px | 180px | 320px | ✅ |
| Main Workspace | 动态 | 600px | ∞ | ❌ |
| Inspector Panel | 280px | 220px | 400px | ✅ |
| Status Bar | 32px (高) | - | - | ❌ |
| Title Bar | 40px (高) | - | - | ❌ |

### 1.3 响应式断点

| 断点 | 宽度 | 布局调整 |
|------|------|----------|
| Desktop XL | ≥1600px | 三栏展开，Inspector 显示详细属性 |
| Desktop | 1200px-1599px | 三栏展开，Inspector 简化 |
| Tablet | 900px-1199px | Device Panel 折叠为图标栏 |
| Mobile | <900px | 单栏，Inspector 变为抽屉 |

---

## 2. 设备面板 (Device Panel) 详细设计

### 2.1 展开状态

```
┌────────────────────────┐
│  🔌 DEVICES            │  ← Header (40px)
├────────────────────────┤
│  + Connect Device      │  ← Action Button (36px)
├────────────────────────┤
│  📱 CONNECTED (2)      │  ← Group Header (32px)
├────────────────────────┤
│ ┌────────────────────┐ │
│ │ 🟢 iPhone 15 Pro   │ │  ← Device Item (64px)
│ │    iOS 17.1        │ │
│ │ [📷] [🔁] [✕]     │ │  ← Quick Actions
│ └────────────────────┘ │
│ ┌────────────────────┐ │
│ │ 🟢 Chrome - Web    │ │  ← Device Item (64px)
│ │    localhost:3000  │ │
│ │ [📷] [🔁] [✕]     │ │
│ └────────────────────┘ │
├────────────────────────┤
│  📴 DISCONNECTED (1)   │  ← Group Header (32px)
├────────────────────────┤
│ ┌────────────────────┐ │
│ │ ⚪ Pixel 7         │ │  ← Device Item (48px)
│ │    Android 14      │ │     (简化版，无缩略图)
│ │        [Connect]   │ │
│ └────────────────────┘ │
├────────────────────────┤
│  📁 SAVED SESSIONS     │  ← Group Header (32px)
├────────────────────────┤
│ ┌────────────────────┐ │
│ │ 📄 Login Test      │ │  ← Session Item (48px)
│ │    3 devices       │ │
│ └────────────────────┘ │
└────────────────────────┘
```

### 2.2 设备项组件规格

**活跃设备项 (64px)**
```typescript
interface ActiveDeviceItemProps {
  device: {
    id: string;
    name: string;
    type: 'web' | 'ios' | 'android' | 'computer';
    status: 'connected' | 'connecting' | 'error';
    info: string;           // 如 "iOS 17.1" 或 "localhost:3000"
    thumbnail?: string;     // 最后截图缩略图 (base64)
    isActive: boolean;      // 是否当前选中
  };
  onSelect: () => void;
  onScreenshot: () => void;
  onRefresh: () => void;
  onDisconnect: () => void;
}
```

**样式规范**
```less
.device-item {
  height: 64px;
  padding: 8px 12px;
  border-radius: 8px;
  margin: 4px 8px;
  
  // 状态样式
  &.connected {
    border-left: 3px solid var(--omni-success-color);
  }
  
  &.active {
    background: var(--omni-bg-tertiary);
    box-shadow: 0 0 0 1px var(--omni-accent-color);
  }
  
  // 缩略图
  .thumbnail {
    width: 48px;
    height: 48px;
    border-radius: 4px;
    object-fit: cover;
    border: 1px solid var(--omni-border-color);
  }
  
  // 快捷操作按钮 (hover 显示)
  .quick-actions {
    opacity: 0;
    transition: opacity 0.2s;
  }
  
  &:hover .quick-actions {
    opacity: 1;
  }
}
```

### 2.3 折叠状态

```
┌────┐
│ 🔌 │  ← 图标按钮，hover 显示 "Devices"
├────┤
│ 📱 │  ← 设备类型图标
│ 🟢 │  ← 连接状态指示
├────┤
│ 🌐 │
│ 🟢 │
├────┤
│ 📱 │
│ ⚪ │
└────┘
```

折叠后宽度：48px

---

## 3. 主工作区 (Main Workspace) 详细设计

### 3.1 Playground 模式布局

```
┌────────────────────────────────────────────────────────────┐
│  Toolbar (48px)                                            │
│  [Model: GPT-4o ▼] [History ▼]    [⚙️ Config] [🗑️ Clear]  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Chat History Area                                         │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 👤 User                                             │  │
│  │ 点击登录按钮                                         │  │
│  │                                    10:23            │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ 🤖 AI                                               │  │
│  │ [思考中...]                                         │  │
│  │                                                    │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ 👤 User                                             │  │
│  │ 输入用户名 admin                                    │  │
│  │                                    10:24            │  │
│  ├─────────────────────────────────────────────────────┤  │
│  │ 🤖 AI                                               │  │
│  │ ✅ 已完成输入                                       │  │
│  │ [截图展示]                                          │  │
│  │ [定位到输入框的标记]                                 │  │
│  │                                                    │  │
│  │ Actions:                                           │  │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐              │  │
│  │ │ Input   │ │ Click   │ │ Scroll  │              │  │
│  │ └─────────┘ └─────────┘ └─────────┘              │  │
│  │                                    10:24          │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  Input Area (动态高度，最大 200px)                         │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │ 输入指令...                                         │  │
│  │                                                     │  │
│  │ [📎] [🎤]                           [发送 ⏎]      │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

**Chat Message 组件规格**
```typescript
interface ChatMessageProps {
  message: {
    id: string;
    role: 'user' | 'assistant' | 'system';
    content: string;
    timestamp: Date;
    status: 'sending' | 'sent' | 'error';
    attachments?: {
      type: 'screenshot' | 'element' | 'action';
      data: any;
    }[];
    actions?: {
      type: string;
      label: string;
      icon: string;
      params?: any;
    }[];
  };
}
```

**Message Bubble 样式**
```less
.message-bubble {
  max-width: 85%;
  padding: 12px 16px;
  border-radius: 12px;
  
  &.user {
    background: var(--omni-accent-color);
    color: white;
    margin-left: auto;
    border-bottom-right-radius: 4px;
  }
  
  &.assistant {
    background: var(--omni-bg-tertiary);
    border: 1px solid var(--omni-border-color);
    border-bottom-left-radius: 4px;
  }
  
  // 截图容器
  .screenshot-container {
    margin: 8px -4px;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid var(--omni-border-color);
    
    img {
      max-width: 100%;
      display: block;
    }
    
    // 元素高亮标记
    .element-marker {
      position: absolute;
      border: 2px solid var(--omni-accent-color);
      background: rgba(24, 144, 255, 0.1);
      border-radius: 4px;
    }
  }
  
  // 操作按钮组
  .action-buttons {
    display: flex;
    gap: 8px;
    margin-top: 12px;
    flex-wrap: wrap;
    
    .action-btn {
      padding: 6px 12px;
      border-radius: 6px;
      background: var(--omni-bg-secondary);
      border: 1px solid var(--omni-border-color);
      font-size: 12px;
      cursor: pointer;
      
      &:hover {
        border-color: var(--omni-accent-color);
      }
    }
  }
}
```

### 3.2 Report 模式布局

```
┌────────────────────────────────────────────────────────────┐
│  Report Header (48px)                                      │
│  [📊 Test Report] [🔄 Replay] [💾 Export] [📤 Share]      │
├────────────────────────────────────────────────────────────┤
│  Timeline (60px)                                           │
│  ○────●────○────○────○────○────○────○                    │
│  🖱️   ⌨️   👁️   🖱️   📜   👁️   ✅                         │
│  点击  输入  查看  点击  滚动  查看  断言                    │
│       ▲                                                    │
│    当前选中                                                 │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Detail View                                               │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                                                     │  │
│  │              Screenshot Viewport                    │  │
│  │                                                     │  │
│  │     ┌─────────────────────────────┐                │  │
│  │     │                             │                │  │
│  │     │    [高亮区域]               │                │  │
│  │     │                             │                │  │
│  │     │          [点击位置] ●       │                │  │
│  │     │                             │                │  │
│  │     └─────────────────────────────┘                │  │
│  │                                                     │  │
│  │  Zoom: [25%] [50%] [100%] [150%] [Fit]            │  │
│  │                                                     │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  Task Info Panel (底部，可折叠)                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 📋 Action: Input                                    │  │
│  │ 🎯 Target: Username Input Field                     │  │
│  │ 💭 Thought: 我需要输入用户名来登录...                │  │
│  │                                                     │  │
│  │ ⏱️ Duration: 1.2s    💰 Tokens: 245               │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Timeline 组件规格**
```typescript
interface TimelineProps {
  tasks: {
    id: string;
    type: 'action' | 'insight' | 'planning' | 'assertion';
    status: 'pending' | 'running' | 'completed' | 'error';
    icon: string;
    label: string;
    timestamp: number;
  }[];
  currentTaskId: string | null;
  onTaskSelect: (taskId: string) => void;
}
```

**Timeline 样式**
```less
.timeline {
  display: flex;
  align-items: center;
  padding: 12px 24px;
  gap: 4px;
  overflow-x: auto;
  
  .timeline-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    padding: 8px 12px;
    border-radius: 8px;
    cursor: pointer;
    min-width: 60px;
    
    &.active {
      background: var(--omni-bg-tertiary);
      box-shadow: 0 0 0 2px var(--omni-accent-color);
    }
    
    &.error {
      color: var(--omni-error-color);
    }
    
    .icon {
      width: 24px;
      height: 24px;
      font-size: 16px;
    }
    
    .label {
      font-size: 11px;
      white-space: nowrap;
    }
  }
  
  // 连接线
  .connector {
    flex: 1;
    height: 2px;
    background: var(--omni-border-color);
    min-width: 20px;
    max-width: 40px;
    
    &.completed {
      background: var(--omni-success-color);
    }
  }
}
```

### 3.3 Recorder 模式布局

```
┌────────────────────────────────────────────────────────────┐
│  Recorder Header (48px)                                    │
│  [⏺️ Recording...] [⏸️ Pause] [⏹️ Stop]  00:02:34         │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  Current Recording                                         │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 📄 Session: New Recording                           │  │
│  │ 📝 Actions: 12                                      │  │
│  │ ⏱️ Duration: 02:34                                  │  │
│  │                                                     │  │
│  │ Real-time Preview:                                  │  │
│  │ ┌───────────────────────────────────────────────┐  │  │
│  │ │ 1. Click - Login Button                       │  │  │
│  │ │ 2. Input - username: "admin"                  │  │  │
│  │ │ 3. Click - Submit                             │  │  │
│  │ │ ...                                           │  │  │
│  │ └───────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  Session List                                              │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ ☑️ [📝 Login Test] [12] [02:34] [▶️] [💾] [🗑️]    │  │
│  │ □  [📝 Search Flow] [8]  [01:45] [▶️] [💾] [🗑️]    │  │
│  │ □  [📝 Checkout]    [15] [03:12] [▶️] [💾] [🗑️]    │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  Export Options (底部固定)                                 │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ Export as: [YAML] [Playwright] [Puppeteer] [CSV]   │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 4. 检查器面板 (Inspector Panel) 详细设计

### 4.1 Playground 模式

```
┌────────────────────────┐
│ 👁️ INSPECTOR           │
├────────────────────────┤
│ 📱 Device Preview      │
│ ┌────────────────────┐ │
│ │                    │ │
│ │  [Screenshot]      │ │
│ │                    │ │
│ │  393 × 852 @3x     │ │
│ └────────────────────┘ │
│ [📷 Capture] [🔄]     │
├────────────────────────┤
│ ℹ️ Device Info         │
│ Type: iPhone 15 Pro   │
│ OS: iOS 17.1          │
│ Resolution: 393×852   │
│ Scale: 3x             │
├────────────────────────┤
│ 🤖 Model Config        │
│ Provider: OpenAI      │
│ Model: GPT-4o         │
│ Temperature: 0.7      │
│ [Change...]           │
├────────────────────────┤
│ 📊 Usage               │
│ Tokens: 1,245         │
│ Cost: $0.003          │
│ Latency: 1.2s avg     │
└────────────────────────┘
```

### 4.2 Report 模式

```
┌────────────────────────┐
│ 👁️ TASK DETAILS        │
├────────────────────────┤
│ 📋 Action Information  │
│ Type: Input           │
│ Status: ✅ Completed  │
│ Duration: 1.2s        │
├────────────────────────┤
│ 🎯 Target Element      │
│ Tag: input            │
│ ID: username          │
│ Class: form-control   │
│ Text: -               │
│ Coordinates: (120, 450)│
├────────────────────────┤
│ 💭 AI Reasoning        │
│ "用户想要输入用户名， │
│  我需要在登录表单中   │
│  找到用户名输入框..." │
├────────────────────────┤
│ 📊 Model Response      │
│ Prompt: 245 tokens    │
│ Completion: 78 tokens │
│ Total: 323 tokens     │
├────────────────────────┤
│ 🔧 Technical Details   │
│ [View Raw JSON]       │
│ [Copy Dump]           │
└────────────────────────┘
```

### 4.3 可折叠设计

折叠后显示为图标栏（48px 宽）：
```
┌────┐
│ 👁️ │  ← Inspector
├────┤
│ 📱 │  ← 点击展开 Device Preview
├────┤
│ ℹ️ │  ← 点击展开 Info
├────┤
│ 🤖 │  ← 点击展开 Model
├────┤
│ 📊 │  ← 点击展开 Usage
└────┘
```

---

## 5. 状态栏 (Status Bar) 详细设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  🟢  [连接状态]  │  📱 iPhone 15 Pro  │  📐 393×852 @3x  │  🤖 GPT-4o    │
│                                                                             │
│  ├─────────────┼───────────────────┼─────────────────┼─────────────────┤  │
│                                                                             │
│  [状态指示器]   [设备信息]          [屏幕规格]        [模型信息]          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

**状态栏组件**
```typescript
interface StatusBarProps {
  connection: {
    status: 'connected' | 'disconnected' | 'connecting' | 'error';
    latency?: number;      // ms
  };
  device: {
    name: string;
    type: 'web' | 'ios' | 'android' | 'computer';
    resolution: { width: number; height: number; scale: number };
    osVersion?: string;
  } | null;
  model: {
    name: string;
    provider: string;
  };
  tasks: {
    pending: number;
    running: number;
    completed: number;
  };
}
```

**状态栏样式**
```less
.status-bar {
  height: 32px;
  background: var(--omni-bg-secondary);
  border-top: 1px solid var(--omni-border-color);
  display: flex;
  align-items: center;
  padding: 0 16px;
  gap: 16px;
  font-size: 12px;
  
  .status-section {
    display: flex;
    align-items: center;
    gap: 6px;
    
    &:not(:last-child) {
      border-right: 1px solid var(--omni-border-color);
      padding-right: 16px;
    }
  }
  
  .status-indicator {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    
    &.connected { background: var(--omni-success-color); }
    &.disconnected { background: var(--omni-text-secondary); }
    &.connecting { 
      background: var(--omni-warning-color);
      animation: pulse 1.5s infinite;
    }
    &.error { background: var(--omni-error-color); }
  }
  
  .task-counter {
    margin-left: auto;
    display: flex;
    gap: 12px;
    
    .counter {
      display: flex;
      align-items: center;
      gap: 4px;
      
      &.pending { color: var(--omni-warning-color); }
      &.running { color: var(--omni-accent-color); }
      &.completed { color: var(--omni-success-color); }
    }
  }
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
```

---

## 6. 拖拽和调整大小

### 6.1 拖拽手柄设计

```
│     ║     │  ← 垂直拖拽手柄 (Device Panel ↔ Main)
     ↑
  拖拽光标: col-resize

──────╱──────  ← 水平拖拽手柄 (Main ↔ Inspector)
      ↑
   拖拽光标: row-resize (当 Inspector 在底部时)
```

**拖拽手柄样式**
```less
.resize-handle {
  width: 4px;
  background: transparent;
  cursor: col-resize;
  transition: background 0.2s;
  
  &:hover,
  &[data-resize-handle-active] {
    background: var(--omni-accent-color);
  }
  
  // 拖拽指示器
  &::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 2px;
    height: 32px;
    background: var(--omni-border-color);
    border-radius: 1px;
    opacity: 0;
    transition: opacity 0.2s;
  }
  
  &:hover::after {
    opacity: 1;
  }
}
```

### 6.2 面板尺寸记忆

```typescript
// 使用 localStorage 保存用户调整的尺寸
interface LayoutState {
  devicePanel: {
    width: number;
    collapsed: boolean;
  };
  inspectorPanel: {
    width: number;
    collapsed: boolean;
  };
  playground: {
    inputHeight: number;
  };
}

const LAYOUT_STORAGE_KEY = 'omni-controller-layout';
```

---

## 7. 动画和过渡

### 7.1 面板折叠动画

```less
.panel {
  transition: width 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  
  &.collapsed {
    width: 48px !important;
  }
  
  .panel-content {
    transition: opacity 0.2s;
    opacity: 1;
  }
  
  &.collapsed .panel-content {
    opacity: 0;
    pointer-events: none;
  }
  
  &.collapsed .collapsed-icon {
    opacity: 1;
  }
}
```

### 7.2 消息动画

```less
.message-enter {
  animation: messageSlideIn 0.3s ease-out;
}

@keyframes messageSlideIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

// 思考中动画
.thinking-indicator {
  display: flex;
  gap: 4px;
  
  .dot {
    width: 8px;
    height: 8px;
    background: var(--omni-accent-color);
    border-radius: 50%;
    animation: thinkingBounce 1.4s infinite ease-in-out both;
    
    &:nth-child(1) { animation-delay: -0.32s; }
    &:nth-child(2) { animation-delay: -0.16s; }
  }
}

@keyframes thinkingBounce {
  0%, 80%, 100% { transform: scale(0); }
  40% { transform: scale(1); }
}
```

### 7.3 截图高亮动画

```less
.element-highlight {
  position: absolute;
  border: 2px solid var(--omni-accent-color);
  background: rgba(24, 144, 255, 0.1);
  border-radius: 4px;
  animation: highlightPulse 2s infinite;
  pointer-events: none;
}

@keyframes highlightPulse {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(24, 144, 255, 0.4);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(24, 144, 255, 0);
  }
}
```

---

## 8. 深色/浅色主题变量

```less
// themes/variables.less
:root {
  // 基础颜色
  --omni-accent-color: #1890ff;
  --omni-success-color: #52c41a;
  --omni-warning-color: #faad14;
  --omni-error-color: #f5222d;
  
  // 浅色主题 (默认)
  &[data-theme='light'] {
    --omni-bg-primary: #ffffff;
    --omni-bg-secondary: #f5f5f5;
    --omni-bg-tertiary: #fafafa;
    --omni-bg-elevated: #ffffff;
    
    --omni-text-primary: rgba(0, 0, 0, 0.85);
    --omni-text-secondary: rgba(0, 0, 0, 0.45);
    --omni-text-tertiary: rgba(0, 0, 0, 0.25);
    
    --omni-border-color: #e8e8e8;
    --omni-border-light: #f0f0f0;
    
    --omni-shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
    --omni-shadow-md: 0 4px 12px rgba(0, 0, 0, 0.1);
    --omni-shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.15);
  }
  
  // 深色主题
  &[data-theme='dark'] {
    --omni-bg-primary: #1a1a1a;
    --omni-bg-secondary: #252525;
    --omni-bg-tertiary: #2d2d2d;
    --omni-bg-elevated: #333333;
    
    --omni-text-primary: rgba(255, 255, 255, 0.95);
    --omni-text-secondary: rgba(255, 255, 255, 0.65);
    --omni-text-tertiary: rgba(255, 255, 255, 0.45);
    
    --omni-border-color: #3d3d3d;
    --omni-border-light: #2d2d2d;
    
    --omni-shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.2);
    --omni-shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
    --omni-shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.6);
  }
}
```

---

## 9. 响应式布局适配

### 9.1 平板模式 (< 1200px)

```
┌─────────────────────────────────────────────┐
│  Title Bar                                  │
├──────┬──────────────────────────────────────┤
│      │                                      │
│  D   │      Main Workspace                  │
│  E   │                                      │
│  V   │  ┌────────────────────────────────┐  │
│  I   │  │                                │  │
│  C   │  │     Content                    │  │
│  E   │  │                                │  │
│      │  └────────────────────────────────┘  │
│  48px│                                      │
│  Icon│  ┌────────────────────────────────┐  │
│  Bar │  │      Input / Timeline          │  │
│      │  └────────────────────────────────┘  │
├──────┴──────────────────────────────────────┤
│  Status Bar                                 │
└─────────────────────────────────────────────┘
```

- Device Panel 折叠为 48px 图标栏
- Inspector Panel 变为底部抽屉，点击展开
- Timeline 保持水平滚动

### 9.2 手机模式 (< 768px)

```
┌─────────────────────────────┐
│ Title Bar                   │
├─────────────────────────────┤
│                             │
│  Main Workspace             │
│                             │
│  ┌────────────────────────┐ │
│  │                        │ │
│  │    Content             │ │
│  │                        │ │
│  └────────────────────────┘ │
│                             │
│  ┌────────────────────────┐ │
│  │  Input                 │ │
│  └────────────────────────┘ │
├─────────────────────────────┤
│ Bottom Navigation           │
│ [Devices] [Chat] [Inspect]  │
└─────────────────────────────┘
```

- 底部导航栏切换视图
- 全屏模式查看截图
- 简化 Inspector 信息

---

## 10. 字体和排版

### 10.1 字体规范

```less
// 字体族
--omni-font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 
                    'Helvetica Neue', Arial, sans-serif;
--omni-font-mono: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', monospace;

// 字体大小
--omni-font-xs: 11px;    // 状态栏、标签
--omni-font-sm: 12px;    // 辅助文字、时间戳
--omni-font-base: 14px;  // 正文
--omni-font-md: 16px;    // 标题
--omni-font-lg: 20px;    // 大标题
--omni-font-xl: 24px;    // 页面标题

// 行高
--omni-line-tight: 1.25;
--omni-line-base: 1.5;
--omni-line-relaxed: 1.75;

// 字重
--omni-weight-normal: 400;
--omni-weight-medium: 500;
--omni-weight-semibold: 600;
--omni-weight-bold: 700;
```

### 10.2 间距规范

```less
// 间距系统 (4px 基数)
--omni-space-1: 4px;
--omni-space-2: 8px;
--omni-space-3: 12px;
--omni-space-4: 16px;
--omni-space-5: 20px;
--omni-space-6: 24px;
--omni-space-8: 32px;
--omni-space-10: 40px;
--omni-space-12: 48px;

// 圆角
--omni-radius-sm: 4px;
--omni-radius-md: 8px;
--omni-radius-lg: 12px;
--omni-radius-xl: 16px;
--omni-radius-full: 9999px;
```

---

## 11. 图标规范

使用统一的图标库（建议使用 `@ant-design/icons` 或 `lucide-react`）：

| 功能 | 图标 | 名称 |
|------|------|------|
| 设备连接 | 🔌 | Plug |
| Web | 🌐 | Globe |
| iOS | 📱 | Smartphone |
| Android | 🤖 | Bot |
| Computer | 💻 | Monitor |
| 截图 | 📷 | Camera |
| 刷新 | 🔄 | RefreshCw |
| 断开 | ✕ | X |
| 播放 | ▶️ | Play |
| 暂停 | ⏸️ | Pause |
| 停止 | ⏹️ | Square |
| 录制 | ⏺️ | Circle |
| 设置 | ⚙️ | Settings |
| 主题 | 🌙/☀️ | Moon/Sun |
| 导出 | 💾 | Save |
| 分享 | 📤 | Share |
| 清空 | 🗑️ | Trash |

---

## 12. 布局组件代码示例

### 12.1 主布局组件

```tsx
// components/layout/AppLayout.tsx
import { Panel, PanelGroup, PanelResizeHandle } from 'react-resizable-panels';
import { useState } from 'react';

export function AppLayout() {
  const [devicePanelCollapsed, setDevicePanelCollapsed] = useState(false);
  const [inspectorCollapsed, setInspectorCollapsed] = useState(false);

  return (
    <div className="app-layout">
      <TitleBar />
      
      <PanelGroup direction="horizontal" className="main-layout">
        {/* Device Panel */}
        <Panel
          defaultSize={20}
          minSize={15}
          maxSize={25}
          collapsible
          collapsedSize={3}
          onCollapse={() => setDevicePanelCollapsed(true)}
          onExpand={() => setDevicePanelCollapsed(false)}
        >
          <DevicePanel collapsed={devicePanelCollapsed} />
        </Panel>
        
        <PanelResizeHandle className="resize-handle" />
        
        {/* Main Workspace */}
        <Panel defaultSize={60} minSize={40}>
          <MainWorkspace />
        </Panel>
        
        <PanelResizeHandle className="resize-handle" />
        
        {/* Inspector Panel */}
        <Panel
          defaultSize={20}
          minSize={15}
          maxSize={30}
          collapsible
          collapsedSize={3}
          onCollapse={() => setInspectorCollapsed(true)}
          onExpand={() => setInspectorCollapsed(false)}
        >
          <InspectorPanel collapsed={inspectorCollapsed} />
        </Panel>
      </PanelGroup>
      
      <StatusBar />
    </div>
  );
}
```

---

*文档版本: 1.0*
*更新日期: 2024*

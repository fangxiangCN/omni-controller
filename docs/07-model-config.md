# Midscene 模型配置

默认推荐（参考 Midscene README）：

MIDSCENE_MODEL_NAME=Qwen3-VL
MIDSCENE_OPENAI_BASE_URL=https://api.openai.com/v1
MIDSCENE_OPENAI_API_KEY=your_api_key_here

说明:
- 如果你使用 Qwen-VL 的兼容服务，可保持模型名为 Qwen3-VL 或改为服务端支持的名称
- 将 MIDSCENE_OPENAI_API_KEY 替换为你的 Key
- 如使用兼容 OpenAI 的服务，替换 BASE_URL
